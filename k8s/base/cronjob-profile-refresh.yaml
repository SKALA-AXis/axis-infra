# 작성일: 2026-05-21
# 작성자: 최종민
# 변경이력:
#   2026-05-21 최종민 — profile-refresh CronJob 신설, Degraded 복구 및 셧다운 시간대 밖으로 스케줄 이동
#   2026-05-26 박지원 — track-a 외 LLM cronjob suspend 및 profile·trend cronjob 정비
# W2-2 · 분기 1회 (분기 첫날 03:00 KST) peer profile_snapshot 갱신.
#   · axis-ai 컨테이너에서 scripts/refresh_peer_profile_snapshots.py 실행.
#   · PeerProfileAgent(ProfileInputBuilder → EvidenceSelector → SnapshotSummarizer → Validator)
#     합성 → peer_companies.profile_snapshot JSONB 저장.
#   · 비용: 5 peer × gpt-4o-mini 기준 수십 원 수준/분기.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: axis-cron-profile-refresh
  namespace: skala3-finalproj-class3-team13
  labels:
    app.kubernetes.io/component: cron
    app.kubernetes.io/part-of: axis
spec:
  # 분기 1회 (1/4/7/10월 1일 03:00 KST). peer 5 사 × PeerProfileAgent.
  suspend: false
  schedule: "30 10 1 1,4,7,10 *"  # 노드 야간 셧다운(23:00-07:00 KST) 회피 — docs/adr/0007 참조
  timeZone: "Asia/Seoul"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 5400  # 90 분
      ttlSecondsAfterFinished: 7200
      template:
        metadata:
          labels:
            app.kubernetes.io/component: cron
            cron: profile-refresh
        spec:
          restartPolicy: OnFailure
          serviceAccountName: axis-ai-sa
          containers:
            - name: refresh
              image: REPLACE_ACCOUNT.dkr.ecr.ap-northeast-2.amazonaws.com/axis-ai:REPLACE_TAG
              command: ["python", "scripts/refresh_peer_profile_snapshots.py"]
              # `python scripts/x.py` 는 sys.path[0] 에 /app/scripts 만 올려서
              # 프로젝트 루트(/app)의 `src` 패키지를 못 찾는다(ModuleNotFoundError).
              # 서비스 컨테이너엔 PYTHONPATH 가 비어있으므로 크론에서 /app 을 명시한다.
              env:
                - name: PYTHONPATH
                  value: /app
              envFrom:
                - configMapRef:
                    name: axis-config
                - secretRef:
                    name: axis-secrets
              resources:
                requests:
                  cpu: 500m
                  memory: 1Gi
                limits:
                  cpu: 1500m
                  memory: 3Gi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                runAsNonRoot: true
                runAsUser: 65534
