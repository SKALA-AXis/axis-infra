# 작성일: 2026-06-09
# 작성자: 최종민
# 변경이력:
#   2026-06-09 최종민 — CronJob 에 Harbor pull secret(harbor-creds) 주입 패치 추가 (cold node ImagePullBackOff 방지)
# Harbor private 이미지(axis-ai / axis-ai-cron) CronJob — cold node ImagePullBackOff 방지.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: placeholder
spec:
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
            - name: harbor-creds
